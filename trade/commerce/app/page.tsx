/** Standalone Commerce entry; enterprise panels reuse the same business controls. */
import { CommerceWorkspace } from './workspace.tsx'

/** Render the standalone login and workspace.
 * @returns Commerce UI.
 */
export default function Page() { return <CommerceWorkspace/> }
